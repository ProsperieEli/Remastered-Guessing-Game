export function 

const correctNumber = math.floor(math.random * 18));
compareNumbers(guess, correctNumber){
    if (guess === correctNumber){
        return myResults.textContent = 'You win!';
    } else if (guess < correctNumber){
        myHints.textContent = 'Riddle me this: What is below the age to drink, but after the 90s?';
        highorLow.textContent = 'Too low.';
        return myResults.textContent = 'You lose!';
        
    } else if (guess > correctNumber){
        
    }
}